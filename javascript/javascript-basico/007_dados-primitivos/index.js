// String, number, undefined, null, boolean

const nome = 'Caique'; // String
const nome1 = "Caique"; // String
const nome2 = `Caique`; // String
const num1 = 10; // number
const num2 = 10.52 // number
let nomeAluno; // undefined = não aponta pra lugar nenhum na memória
let sobrenomeAluno = null; // Nulo = não aponta pra lugar nenhum na memória
const aprovado = true; // Boolean = true, false (lógico)

